# QA Report (Gira_MAX)

Дата: 2026-03-22

## Автопроверки

- `python3 -m compileall -q .` -> OK
- `.venv/bin/python -c "import main"` -> OK
- `.venv/bin/pytest -q` -> 3 passed

## Что покрыто автотестами

1. Префиксы категорий админ-чата.
2. Отсутствие `message_thread_id` в `handlers/*.py`.
3. Отсутствие внешней зависимости `redis` в `requirements.txt`.

## Что требует ручного прогона (боевой checklist)

- Регистрация арендатора (валидный/невалидный ИНН).
- Полный цикл подачи показаний (вода/электричество, редактирование, сохранение).
- Техзаявка (текст, фото/видео, лимиты вложений).
- Уведомления о расторжении и ремонтные работы.
- Платежные сценарии (tenant/landlord callbacks + spam scheduler stop/start).
- Админские сценарии (`/admin`, рассылка, управление пользователями, подача показаний, документы).
- Планировщик ежемесячных задач в реальном времени.
- Очистка legacy `business_documents` после backup.

## Текущий статус

- Техническая миграция и стартовый запуск: **готово**.
- Полный функциональный паритет по всем бизнес-сценариям: **требуется ручной E2E прогон на боевом стенде MAX**.
